import ContentLayout from "../../layouts/ContentLayout";

const TechStack = () => {
  return <div>TechStack</div>;
};

TechStack.getLayout = (page) => {
  return <ContentLayout>{page}</ContentLayout>;
};

export default TechStack;
